# Arquivo criado automaticamente para resolver Include ausente
# As diretivas LoadModule e PHPIniDir já existem em httpd.conf principal.
# Mantemos apenas o AddHandler para garantir processamento de .php neste diretório.
AddHandler application/x-httpd-php .php
